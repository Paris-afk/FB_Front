import * as React from "react";

// This page allows the user to adjust their preferences.
const Settings: React.FC = () => (
  <div className="container mt-4">
    <h2 style={{ color: '#1877f2' }}>Paramètres</h2>
    <p className="mt-3">Ici, vous pouvez ajuster vos préférences pour le clone de Facebook. (Ceci est une démo, donc les paramètres ne sont pas fonctionnels.)</p>
  </div>
);

export default Settings;
